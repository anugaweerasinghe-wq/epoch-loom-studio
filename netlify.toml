[build]
  command = "npm run build"
  publish = ".vercel/output/static"   # Nitro still outputs .vercel, but Netlify reads it
  functions = ".vercel/output/functions"

[[redirects]]
  from = "/*"
  to = "/.netlify/functions/server"
  status = 200
  force = true
