[build]
  command = "npm run build"
  publish = "dist"                    # Nitro with netlify-edge outputs to "dist"
  functions = "dist/server"

[[redirects]]
  from = "/*"
  to = "/.netlify/functions/server"
  status = 200
  force = true
